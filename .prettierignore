# Build output and coverage reports.
dist
coverage
*.tsbuildinfo

node_modules

# npm owns this file's formatting; reformatting it would fight every `npm install`.
package-lock.json

# Written by `npm run test:ci` for the workflow to read, and gitignored.
jest-results.json

# 1.5MB of exchange calendar, regenerated by `refresh-market-hours` rather than edited.
# Reformatting it would rewrite the whole file and say nothing about it.
packages/marketdata/src/market-hours-data.json

# Session notes, not part of the codebase.
prompts/

# Copied to credentials.ts by hand; kept as-is so the copy is a copy.
packages/playground/src/credentials.example.ts

# Broker payloads captured verbatim from Alpaca, kept as they arrived so a fixture is
# evidence of what the wire actually looked like rather than of how we reformat it.
packages/playground/data/

# Prose, hand-wrapped. Prettier reflows Markdown to its own width and would rewrite
# every paragraph in these documents to say nothing about any of them. The rules the
# code is held to live in md/GUIDELINES.md; the document itself is written, not built.
*.md

# Chart data and rendered PNGs, written by the playground scripts and regenerated on
# demand. Python and TOML are not prettier's to format either.
viz/
