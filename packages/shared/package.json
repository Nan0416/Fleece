{
  "name": "@fleece/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Domain models, API contracts, error types and utilities shared by every Fleece package.",
  "engines": {
    "node": ">=22"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3"
  },
  "license": "MIT",
  "dependencies": {
    "decimal.js": "^10.6.0"
  }
}
