{
  "name": "@fleece/utilities",
  "version": "1.0.0",
  "private": true,
  "description": "Exact decimals, the clock, logging, environment reading, assertions, error types, the HTTP client seam and the mini-cloud job runner. Imports nothing of ours.",
  "engines": {
    "node": ">=22"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@mini-cloud/reporter": "^1.0.0",
    "decimal.js": "^10.6.0",
    "moment-timezone": "^0.6.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3"
  },
  "license": "MIT"
}
