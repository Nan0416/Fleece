{
  "name": "@fleece/models",
  "version": "1.0.0",
  "private": true,
  "description": "The domain model — accounts, positions, orders, broker events — and the HTTP request and response contracts written against it.",
  "engines": {
    "node": ">=22"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@fleece/utilities": "1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3"
  },
  "license": "MIT"
}
